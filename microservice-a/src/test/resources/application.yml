spring:
  datasource:
    url: jdbc:h2:mem:att_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

att:
  mocks:
    operador-url: http://localhost:4010
    recibo-url: http://localhost:4011
  services:
    customer-profile-url: http://localhost:8081
