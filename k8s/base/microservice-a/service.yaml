apiVersion: v1
kind: Service
metadata:
  name: microservice-a
  labels:
    app: microservice-a
    team: paymentbox-core
spec:
  selector:
    app: microservice-a
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
