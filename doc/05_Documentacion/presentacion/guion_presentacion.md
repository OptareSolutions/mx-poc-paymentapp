# Gui├│n de Presentaci├│n ÔÇö PoC AT&T Quality Assurance> **Cliente:** AT&T (Telco Operator)  > **Proyecto:** PaymentBox ÔÇö PoC de Calidad Declarativa  > **Versi├│n:** 1.0  > **Fecha:** Mayo 2026  > **Presentador:** Optare Solutions ÔÇö QE & DevSecOps  > **Duraci├│n estimada total:** 45ÔÇô60 minutos---## Checklist Pre-Demo (30 min antes)- [ ] Entorno simulado levantado: `cd simulation && docker compose up -d`- [ ] Todos los servicios sanos: `docker compose ps` ÔåÆ todos "healthy"- [ ] Repositorio GitHub abierto en Chrome/Edge (tab 1)- [ ] GitHub Actions tab abierto con ├║ltimo run verde (tab 2)- [ ] Swagger UI microservice-a abierto: `http://localhost:8080/swagger-ui.html` (tab 3)- [ ] Swagger UI microservice-b abierto: `http://localhost:8081/swagger-ui.html` (tab 4)- [ ] UI PaymentBox abierta: `http://localhost:4200` (tab 5)- [ ] SonarCloud dashboard abierto (tab 6)- [ ] VS Code abierto en la carpeta del repositorio- [ ] PowerShell listo en la ra├¡z del repositorio---## Estructura de la Presentaci├│n| Bloque | Contenido | Tiempo ||--------|-----------|--------|| 1 | Contexto y problem├ítica AT&T | 5 min || 2 | Arquitectura de la soluci├│n | 8 min || 3 | DEMO: Flujo verde (pipeline OK) | 10 min || 4 | DEMO BREAK 1: Ruptura de contrato | 8 min || 5 | DEMO BREAK 2: Ruptura de comportamiento | 8 min || 6 | Escenario Performance (k6) | 5 min || 7 | Framework y reporteo unificado | 5 min || 8 | Q&A y diferenciaci├│n Optare | 10 min |---## Bloque 1 ÔÇö Contexto y Problem├ítica AT&T (5 min)### Gui├│n> "AT&T realiza m├ís de 5.200 validaciones manuales para flujos de negocio que incluyen simulaciones de venta, postventa y verificaci├│n de productos. Estas validaciones se distribuyen en f├íbricas de software que gestionan proyectos con mejoras de plataforma y nuevos productos.>> Los principales retos que nos han presentado son tres:>> **RPA:** Las soluciones actuales superan en tiempo a una ejecuci├│n manual en algunos casos, el mantenimiento por cambios es costoso, no existe un framework centralizado y el reporte en tiempo real es complejo.>> **CI/CD:** Las validaciones de seguridad actuales (Veracode, SonarQube) no detectan errores funcionales a tiempo. Los hallazgos se descubren tarde, cuando ya est├ín en ambientes de prueba, generando retrasos significativos.>> **Performance:** JMeter presenta limitaciones con m├ís de 4.000 usuarios virtuales por 3.600 segundos, y la creaci├│n de scripts es manual por la arquitectura de seguridad (tokens entre nubes y ubicaciones).>> Nuestra propuesta resuelve los tres escenarios en un ├║nico flujo declarativo."### Puntos clave a destacar- 5.200 validaciones manuales = costo operativo enorme- La automatizaci├│n existente tiene adopci├│n baja porque es lenta e inestable- No hay framework unificado ÔåÆ fragmentaci├│n de resultados- Nosotros entregamos: calidad desde el primer commit, sin intervenci├│n manual---## Bloque 2 ÔÇö Arquitectura de la Soluci├│n (8 min)### Gui├│n> "Les presento la arquitectura de nuestra PoC. Hemos construido un sistema de **calidad declarativa** donde la calidad no es una etapa final, sino un requisito que cada commit debe cumplir para avanzar al siguiente entorno.>> La soluci├│n tiene tres capas principales."### [Mostrar diagrama arquitectura]> "**Capa de aplicaci├│n:** Dos microservicios Java Spring Boot que simulan el flujo de negocio PaymentBox de AT&T. Un frontend Angular que representa la interfaz del operador. Una base de datos PostgreSQL con perfiles de clientes sint├®ticos.>> **Capa de calidad:** Tres pipelines GitHub Actions que ejecutan autom├íticamente: tests unitarios con cobertura ÔëÑ 80%, an├ílisis de seguridad con Trivy (zero tolerance a CVEs cr├¡ticos), pruebas de API con Karate DSL en tres niveles ÔÇöfuncional, integraci├│n y contratoÔÇö, pruebas de UI con Selenium headless, y smoke performance con k6.>> **Capa GitOps:** Los manifiestos Kubernetes son la ├║nica fuente de verdad. Un pipeline verde es el ├║nico mecanismo que puede actualizar la etiqueta de imagen en el overlay del entorno destino. Si algo falla, el entorno de producci├│n no se toca."### [Mostrar multi-ambiente]> "Tenemos cuatro entornos: develop ÔåÆ qa ÔåÆ uat ÔåÆ main/producci├│n. La promoci├│n entre entornos es siempre manual y declarativa. Nadie puede saltarse la validaci├│n."### Puntos clave- "Shift-Left" real: calidad en el primer commit, no al final del sprint- GitOps: el manifiesto no cambia si hay un fallo ÔåÆ protecci├│n autom├ítica del entorno- Separaci├│n de equipos: Team A y Team B trabajan en paralelo sin bloquearse---## Bloque 3 ÔÇö DEMO: Flujo Verde (10 min)### Gui├│n> "Voy a mostrarles el flujo normal. Un desarrollador hace push a la rama `develop` con un cambio en microservice-a."### [Acci├│n: mostrar un push reciente o ejecutar `workflow_dispatch`]> "El pipeline se activa autom├íticamente. Vamos a ver los cuatro jobs ejecut├índose."### [Mostrar GitHub Actions ÔÇö pipeline-microservice-a]**Job 1 ÔÇö Build & Quality:**> "En menos de 2 minutos tenemos: compilaci├│n, 100% de tests unitarios en verde, y cobertura JaCoCo al 83%. SonarCloud ya est├í analizando el c├│digo. Si la cobertura cae por debajo del 80%, el pipeline falla aqu├¡."**Job 2 ÔÇö Seguridad:**> "Trivy escanea el sistema de archivos: cero vulnerabilidades cr├¡ticas o altas. Si hubiera una CVE en una dependencia, el pipeline se detiene aqu├¡. Nadie construye una imagen insegura."**Job 3 ÔÇö Image Ops:**> "Docker build multi-stage: primero Gradle compila el JAR, luego solo el JRE Alpine se incluye en la imagen final. Trivy vuelve a escanear la imagen. La imagen se sube al registry con el tag `env-e-{sha}`. Cada commit tiene su propia imagen inmutable."**Job 4 ÔÇö E2E + GitOps:**> "Karate ejecuta los 8 pasos del flujo completo: localiza al cliente Billy 1, selecciona el monto, valida contra el mock del operador, registra el pago, verifica en base de datos, emite el recibo. Todo en verde. El pipeline actualiza autom├íticamente el manifiesto Kustomize con la nueva etiqueta de imagen. En un entorno real, ArgoCD sincronizar├¡a esto a Kubernetes."### [Mostrar SonarCloud dashboard]> "Aqu├¡ vemos el hist├│rico de calidad: cobertura, bugs detectados, security hotspots. Es el panel de control del equipo de desarrollo."### Puntos clave- Pipeline completo en < 10 minutos ÔåÆ Lead Time for Changes bajo- Proceso 100% autom├ítico, sin intervenci├│n humana- Evidencia trazable por cada commit---## Bloque 4 ÔÇö DEMO BREAK 1: Ruptura de Contrato (8 min)### Gui├│n> "Ahora voy a mostrarles el escenario m├ís com├║n en ambientes multi-equipo: Team B modifica una API sin coordinar con Team A."### [Acci├│n en VS Code / PowerShell]```powershellcd C:\...\att-poc-paymentboxdemo\break-contract.ps1```> "Hemos renombrado el campo `telefono` a `phone` en el DTO p├║blico de microservice-b. Este cambio es perfectamente v├ílido en los tests unitarios de Team B, que conocen el nuevo nombre. Los tests pasan. La imagen se construye. Pero..."### [Push y mostrar pipelines]```powershellgit add .git commit -m "demo: BREAK 1 - romper contrato DTO"git push origin develop```> "Los pipelines individuales de Team A y Team B pasan en verde, porque cada equipo solo prueba su propio c├│digo. Ahora triggereamos el pipeline de integraci├│n para promover develop a qa."### [Mostrar GitHub Actions ÔÇö pipeline-integration, Job 1 falla en rojo]> "┬íY aqu├¡ lo tenemos! Job 1 ÔÇö Tests de Contrato ÔÇö FALLA. Karate intenta acceder a `response.telefono` que ya no existe. El campo ahora se llama `phone`. La promoci├│n a qa est├í BLOQUEADA.>> Este error, en el mundo real de AT&T, habr├¡a llegado al ambiente de pruebas, habr├¡a requerido un an├ílisis de 2-3 d├¡as para identificar la causa, habr├¡a retrasado el sprint del otro equipo.>> Con nuestra soluci├│n, el error se detecta en **segundos**, antes de que una sola imagen insegura llegue a QA. El equipo responsable recibe la notificaci├│n directa en GitHub."### [Restaurar]```powershelldemo\restore.ps1git add .git commit -m "restore: contrato restaurado"git push origin develop```### Puntos clave- Los contratos de API son ciudadanos de primera clase en el pipeline- La detecci├│n es autom├ítica, no depende de la comunicaci├│n entre equipos- El pipeline es la red de seguridad que protege todos los entornos---## Bloque 5 ÔÇö DEMO BREAK 2: Ruptura de Comportamiento (8 min)### Gui├│n> "El segundo escenario es m├ís sutil y m├ís com├║n: un comportamiento nuevo que los tests unitarios no cubren."### [Acci├│n en VS Code / PowerShell]```powershelldemo\break-behavior.ps1```> "Team A acaba de activar una validaci├│n de negocio: el monto m├¡nimo de recarga es $100. Tiene sentido desde el negocio. Los tests unitarios de Team A se han actualizado para cubrir este caso. Los tests pasan. La imagen se construye y se sube."### [Push a rama qa]```powershellgit add .git commit -m "demo: BREAK 2 - validaci├│n monto m├¡nimo"git push origin qa```> "Aqu├¡ es donde cambia la din├ímica. Jobs 1, 2 y 3 pasan en verde. Todo parece normal. Pero Job 4..."### [Mostrar Job 4 fallando]> "┬íJob 4 falla! Karate intenta ejecutar el Paso 6 con el perfil de prueba Billy 1, recarga de $20. La API responde HTTP 400 ÔÇö el monto es inferior al m├¡nimo. Karate esperaba HTTP 201.>> El overlay `k8s/overlays/env-a` NO se actualiza. El ambiente de QA sigue apuntando a la versi├│n anterior, que funcionaba. El entorno de QA est├í protegido autom├íticamente.>> ┬┐Qu├® pasa ahora? El equipo de QA revisa el reporte Karate, identifica el cambio de comportamiento, coordina con Team A para decidir: ┬┐es intencional? ┬┐Se actualiza el test? ┬┐Se revierte el cambio? La decisi├│n es humana y consciente, no un accidente."### [Restaurar]```powershelldemo\restore.ps1git add .git commit -m "restore: comportamiento restaurado"git push origin qa```### Puntos clave- Los E2E tests son la ├║ltima l├¡nea de defensa antes de GitOps- El entorno no avanza hasta que todo el flujo de negocio es v├ílido- MTTR autom├ítico: rollback es el estado anterior del manifiesto---## Bloque 6 ÔÇö Escenario Performance con k6 (5 min)### Gui├│n> "Para el escenario de performance, usamos k6, que resuelve los problemas que tienen con JMeter."### [Mostrar c├│digo del script k6 en VS Code o ejecutar localmente]> "k6 es una herramienta de performance moderna, scriptada en JavaScript. El script parametriza autom├íticamente los datos de prueba ÔÇöperfiles BillyÔÇö y encadena las peticiones transportando IDs entre respuestas, simulando un flujo real.>> En la PoC, el smoke test corre dentro del pipeline en cada push: 20 usuarios virtuales durante 5 minutos. Si el p95 supera 3 segundos, el pipeline falla antes de llegar a GitOps.>> Para la demo completa de carga, el script est├í configurado para 2.000 VUs durante 3.600 segundos, en protocolo HTTPS. La grabaci├│n por protocolo HTTPS resuelve el problema de los tokens y las conexiones entre distintas nubes que hoy impide la grabaci├│n con JMeter."### [Mostrar output de k6 o ejemplo de reporte]> "El reporte incluye: latencia promedio, distribuci├│n de c├│digos de respuesta, mensajes de error, p95 por endpoint, y comparaci├│n autom├ítica contra la ejecuci├│n anterior para detectar degradaciones."### Puntos clave- k6 supera las limitaciones de JMeter a 4k+ VUs- Grabaci├│n HTTPS nativa ÔåÆ sin problema de tokens y m├║ltiples nubes- Integrado en CI para comparaci├│n hist├│rica autom├ítica---## Bloque 7 ÔÇö Framework y Reporteo Unificado (5 min)### Gui├│n> "Todo lo que hemos visto ÔÇöRPA, CI/CD, API testing, performanceÔÇö est├í unificado en un ├║nico framework con gobierno y est├índares definidos."### [Mostrar GitHub Actions ÔÇö lista de workflows]> "Un solo repositorio, tres pipelines coordinados, cuatro entornos declarativos. El framework incluye:>> **Calendarizaci├│n:** Los pipelines se pueden configurar con cron para ejecuciones nocturnas de regresi├│n completa.>> **Reporteo unificado:** SonarCloud para calidad de c├│digo, JaCoCo para cobertura, Karate HTML para evidencias de API, k6 JSON para performance. Todos los artefactos se almacenan en GitHub Actions como evidencia descargable.>> **Gobierno:** branching declarativo, tags de imagen por entorno, zero-tolerance en seguridad, gates de cobertura. El est├índar est├í codificado en el pipeline, no depende de la disciplina individual del desarrollador.>> **IA integrada:** El framework est├í preparado para integrar capacidades de IA: generaci├│n de casos de prueba desde historias de usuario, datos sint├®ticos bajo demanda m├ís all├í de los perfiles Billy."### Puntos clave- El framework es el activo m├ís valioso, no los scripts individuales- Governance by code ÔåÆ los est├índares no se pueden saltear- Extensible: RPA adicional, m├ís microservicios, m├ís entornos ÔåÆ sin reescribir el framework---## Bloque 8 ÔÇö Q&A y Diferenciaci├│n Optare (10 min)### Puntos de diferenciaci├│n a destacar| Diferenciador | Nuestra propuesta | Alternativa t├¡pica ||--------------|-------------------|-------------------|| **Modelo de calidad** | Declarativo (todo en c├│digo, trazable) | Ad-hoc por equipo || **Detecci├│n de contratos** | En el pipeline, antes de QA | Manual, en QA o producci├│n || **Performance en CI** | Smoke en cada push, comparaci├│n hist├│rica | Pruebas de carga aisladas || **Seguridad** | Zero CVEs CRITICAL/HIGH bloqueante | Escaneo peri├│dico desvinculado || **GitOps** | Manifiestos inmutables, rollback autom├ítico | Deploy manual, rollback manual || **IA** | Framework preparado para generaci├│n de tests | No integrado || **Coste** | Stack 100% open-source (excepto SonarCloud freemium) | Herramientas comerciales (LoadRunner, etc.) |### Respuestas preparadas a posibles preguntas del comit├®**P: ┬┐Por qu├® k6 y no JMeter, que ya conocemos?**> "JMeter tiene limitaciones documentadas por encima de 4k VUs por hora y no soporta grabaci├│n HTTPS con autenticaci├│n compleja entre nubes. k6 est├í dise├▒ado para precisamente esos casos, se integra nativo con GitHub Actions y el scripting en JavaScript es m├ís mantenible para el equipo de QA."**P: ┬┐C├│mo gestionamos los secretos y credenciales de AT&T en el pipeline?**> "GitHub Actions Secrets cifra todas las credenciales en reposo y en tr├ínsito. Los secrets nunca aparecen en logs. Para ambientes de producci├│n, integramos con AWS Secrets Manager o HashiCorp Vault seg├║n la pol├¡tica de AT&T. Los mocks de la PoC no requieren credenciales reales."**P: ┬┐El sistema funciona con nuestra infraestructura actual (m├║ltiples nubes, VPN)?**> "Los runners de GitHub Actions se pueden auto-hospedar (self-hosted runners) dentro de la infraestructura de AT&T, eliminando cualquier restricci├│n de red. La PoC usa runners gestionados por GitHub para simplificar la demo, pero la arquitectura es transparente al proveedor de nube."**P: ┬┐Qu├® sucede si un microservicio no tiene OpenAPI/Swagger definido?**> "Karate funciona con y sin OpenAPI. Para contratos formales, el OpenAPI spec es el contrato. Para APIs sin spec, Karate graba el comportamiento base y lo convierte en el contrato de referencia. La migraci├│n es incremental."**P: ┬┐C├│mo se integra con nuestra herramienta de gesti├│n de defectos (Jira/ServiceNow)?**> "GitHub Actions puede publicar resultados de tests directamente a Jira v├¡a API, crear tickets autom├íticos cuando un gate falla, y cerrarlos cuando el pipeline vuelve a verde. La integraci├│n se configura en el pipeline como un job adicional."**P: ┬┐Cu├ínto tiempo lleva adoptar este framework en nuestros equipos?**> "El framework es incremental. El primer equipo puede estar operativo en 2ÔÇô3 sprints: sprint 1 para el pipeline base con build y tests unitarios, sprint 2 para API testing Karate, sprint 3 para performance y GitOps. Los equipos siguientes heredan el template y se incorporan en 1 sprint."**P: Las acciones de IA, ┬┐son deterministas o pueden introducir variabilidad?**> "Las acciones de IA en el framework est├ín acotadas a tareas de generaci├│n y an├ílisis, no a decisiones de deployment. Los quality gates son siempre deterministas: umbrales num├®ricos, validaciones OpenAPI, resultados de tests. La IA asiste al equipo de QA, no reemplaza los gates."---## Notas del Presentador### Ritmo y energ├¡a- Bloque 3 (flujo verde): mantener ritmo fluido, no detenerse en cada l├¡nea de log- Bloques 4 y 5 (DEMO BREAK): generar tensi├│n dram├ítica ÔÇö el pipeline que falla ES el ├®xito de la demo- Bloque 8 (Q&A): escuchar antes de responder; si no se sabe la respuesta exacta, comprometerse a seguimiento### Si algo falla t├®cnicamente- Si Docker compose falla: tener screenshots/grabaci├│n de pantalla de una ejecuci├│n anterior lista- Si el pipeline tarda m├ís de lo esperado: aprovechar el tiempo para profundizar en la arquitectura con el diagrama- Si la conexi├│n a internet falla: los mocks locales (Prism) funcionan completamente offline### Materiales de respaldo- `C:\Users\jcunha\Documents\Optare\Clientes\AT_T\QA\PoC\att-poc-paymentbox\README.md` ÔÇö detalle t├®cnico completo- `C:\Users\jcunha\Documents\Optare\Clientes\AT_T\QA\05_Documentacion\arquitectura\arquitectura_solucion.md` ÔÇö diagramas de arquitectura- `C:\Users\jcunha\Documents\Optare\Clientes\AT_T\QA\05_Documentacion\tech_stack\tech_stack.md` ÔÇö tech stack justificado- `C:\Users\jcunha\Documents\Optare\Clientes\AT_T\QA\Poc-CiCd.pptx` ÔÇö presentaci├│n PowerPoint existente---## Orden de Demostraci├│n Recomendado```1. Abrir entorno local (docker compose up -d)          ÔåÆ 2 min2. Mostrar README del repo (contexto)                   ÔåÆ 1 min3. Mostrar arquitectura (diagrama)                      ÔåÆ 3 min4. Abrir GitHub Actions ÔåÆ ├║ltimo run verde              ÔåÆ 2 min5. Explicar Job 1 Build+Quality ÔåÆ SonarCloud            ÔåÆ 3 min6. Explicar Job 2 Seguridad ÔåÆ Trivy                     ÔåÆ 2 min7. Explicar Job 3 Image Ops ÔåÆ GHCR                      ÔåÆ 2 min8. Explicar Job 4 E2E Karate ÔåÆ GitOps                   ÔåÆ 3 min9. DEMO BREAK 1: break-contract.ps1 ÔåÆ push ÔåÆ fallo      ÔåÆ 8 min10. DEMO BREAK 2: break-behavior.ps1 ÔåÆ push ÔåÆ fallo     ÔåÆ 8 min11. Mostrar script k6 + m├®tricas                        ÔåÆ 5 min12. Framework + reporteo unificado                      ÔåÆ 5 min13. Q&A                                                 ÔåÆ 10 min```