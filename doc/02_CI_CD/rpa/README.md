# AT&T QA ÔÇô RPA Automation (Playwright + GitHub Actions)Automatizaci├│n web liviana integrada en el pipeline CI/CD usando [Playwright](https://playwright.dev/) en modo headless.---## Estructura```rpa/Ôö£ÔöÇÔöÇ .github/Ôöé   ÔööÔöÇÔöÇ workflows/Ôöé       ÔööÔöÇÔöÇ rpa.yml          # Workflow de GitHub ActionsÔö£ÔöÇÔöÇ scripts/Ôöé   ÔööÔöÇÔöÇ rpa-flow.js          # Script RPA principalÔö£ÔöÇÔöÇ results/                 # Resultados JSON + capturas (generado en runtime)Ôö£ÔöÇÔöÇ package.jsonÔööÔöÇÔöÇ README.md```---## Flujo automatizado| Paso | Acci├│n ||------|--------|| 1 | Navegar a la URL de la aplicaci├│n web || 2 | Login con credenciales (via secretos de GitHub) || 3 | Consultar registro por ID || 4 | Validar que el dato no est├® vac├¡o || - | Guardar resultado JSON + captura de pantalla |---## Configuraci├│n### Variables de entorno / Secretos GitHub| Variable | Descripci├│n | Secret? ||----------|-------------|---------|| `RPA_BASE_URL`  | URL base de la app web     | Ô£à S├¡ || `RPA_USERNAME`  | Usuario de login            | Ô£à S├¡ || `RPA_PASSWORD`  | Contrase├▒a de login         | Ô£à S├¡ || `RPA_RECORD_ID` | ID del registro a consultar | Opcional (default: `1`) || `RPA_TIMEOUT_MS`| Timeout en ms               | Opcional (default: `30000`) |### Configurar secretos en GitHub```Settings ÔåÆ Secrets and variables ÔåÆ Actions ÔåÆ New repository secret```Crear: `RPA_BASE_URL`, `RPA_USERNAME`, `RPA_PASSWORD`---## Ejecuci├│n local```bashcd rpanpm installnpx playwright install chromium --with-deps# Con variables de entorno:RPA_BASE_URL=https://mi-app.com \RPA_USERNAME=user \RPA_PASSWORD=pass \RPA_RECORD_ID=42 \node scripts/rpa-flow.js```---## Ejecuci├│n en pipelineEl workflow se activa en:- **Push** a cualquier rama (excepto `main`) que modifique archivos en `rpa/` o `.github/workflows/rpa.yml`- **Manual** (`workflow_dispatch`) con par├ímetro opcional de `record_id`### Artefactos generadosDisponibles 30 d├¡as en la pesta├▒a **Actions ÔåÆ Artifacts**:- `rpa-result-<timestamp>.json` ÔÇô resultado del flujo con estado de cada paso- `step2-logged-in.png` ÔÇô captura tras login- `step3-record.png` ÔÇô captura del registro consultado- `error-screenshot.png` ÔÇô captura en caso de error (si aplica)---## Seguridad- Acciones pinadas a SHA completo para evitar supply-chain attacks- Credenciales exclusivamente via secretos de GitHub (nunca en c├│digo)- Permisos m├¡nimos: `contents: read`- Chromium headless con `--no-sandbox` para compatibilidad con runners Linux- Retenci├│n de artefactos: 30 d├¡as---## Adaptar el scriptEdita `scripts/rpa-flow.js` y ajusta los selectores CSS en los pasos 2 y 3 seg├║n la aplicaci├│n real:```js// Paso 2 ÔÇô Loginawait page.fill('input[name="username"]', CONFIG.username);// Paso 3 ÔÇô Navegar al registroawait page.goto(`${CONFIG.baseUrl}/records/${CONFIG.recordId}`);```