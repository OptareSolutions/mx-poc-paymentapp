# ­ƒÜÇ k6 Smoke Performance ÔÇö PaymentBox CI/CDIntegraci├│n de k6 en GitHub Actions para pruebas de smoke performance con comparaci├│n hist├│rica de resultados.## Estructura de Archivos```performance/Ôö£ÔöÇÔöÇ smoke_performance.js          # Script k6 principal (20 VUs, 7 min)Ôö£ÔöÇÔöÇ data/Ôöé   ÔööÔöÇÔöÇ users.json                # Datos de prueba parametrizadosÔö£ÔöÇÔöÇ scripts/Ôöé   ÔööÔöÇÔöÇ compare-results.js        # Script de comparaci├│n hist├│ricaÔö£ÔöÇÔöÇ workflows/Ôöé   ÔööÔöÇÔöÇ performance-smoke.yml     # Workflow GitHub ActionsÔööÔöÇÔöÇ README.md                     # Este documento```## Configuraci├│n del Test| Par├ímetro | Valor ||-----------|-------|| Usuarios virtuales (VUs) | 20 || Duraci├│n | 7 minutos || Think time entre pasos | 1ÔÇô3 segundos (aleatorio) || Datos de prueba | Parametrizados (5 perfiles Billy) |## Thresholds Configurados| M├®trica | L├¡mite | Descripci├│n ||---------|--------|-------------|| Flujo completo p95 | < 3000 ms | 95% del flujo de 8 pasos < 3s || Login p95 | < 700 ms | Paso 2: Focalizar cliente || Consulta p95 | < 900 ms | Pasos 3+4: Montos + validaci├│n operador || Actualizaci├│n p95 | < 1200 ms | Pasos 6+7: Registrar pago || Error rate | < 1% | Tasa de errores del flujo completo |## Flujo de 8 Pasos Cubiertos1. _(sin cobertura ÔÇö inicio UI)_2. **Focalizar Cliente** ÔÇö `GET /api/clientes/buscar?telefono=...`3. **Obtener Montos DB** ÔÇö `GET /api/recargas/montos?operador=...`4. **Validar Operador** ÔÇö `POST /api/recargas/validar-operador`5. **M├®todos de Pago** ÔÇö `GET /api/pagos/metodos`6. **Registrar Pago** ÔÇö `POST /api/pagos/registrar` _(ruta cr├¡tica)_7. _(incluido en Paso 6)_8. **Emitir Recibo** ÔÇö `POST /api/recibos/emitir`## Ejecuci├│n Local```bash# Instalar k6: https://k6.io/docs/get-started/installation/# Ejecutar con valores por defecto (1 VU para smoke b├ísico)k6 run tests/k6/smoke_performance.js# Ejecutar con configuraci├│n completak6 run \  --env BASE_URL=http://localhost:8080 \  --env ENVIRONMENT=local \  tests/k6/smoke_performance.js```## Ejecuci├│n en GitHub ActionsEl workflow `performance-smoke.yml` se puede disparar:- **Manual** (`workflow_dispatch`): desde la UI de GitHub Actions con par├ímetros configurables- **Programado** (`schedule`): autom├íticamente cada d├¡a a las 02:00 UTC- **Por llamada** (`workflow_call`): desde `pipeline-integration.yml` como gate de calidad### Par├ímetros del workflow_dispatch| Par├ímetro | Descripci├│n | Valor por defecto ||-----------|-------------|-------------------|| `base_url` | URL del servicio | `http://localhost:8080` || `environment` | Entorno objetivo | `develop` || `vus` | Usuarios virtuales | `20` || `duration` | Duraci├│n del test | `7m` || `update_baseline` | Actualizar baseline | `false` |## Comparaci├│n Hist├│rica### C├│mo funciona1. Cada ejecuci├│n genera `smoke_performance_latest.json` con las m├®tricas2. El baseline se almacena como artefacto de GitHub Actions (`k6-performance-baseline`)3. En cada ejecuci├│n se descarga el baseline y se compara con los resultados actuales4. Si hay regresi├│n (> 20% de deterioro), el job falla con detalle del problema5. El baseline se actualiza autom├íticamente en ejecuciones programadas si todos los thresholds pasan### Actualizar el Baseline ManualmenteEjecutar el workflow con el par├ímetro `update_baseline = true`. Solo se actualiza si todos los thresholds pasan.### Script de Comparaci├│n Local```bashnode tests/k6/scripts/compare-results.js \  --current  tests/k6/results/smoke_performance_latest.json \  --baseline tests/k6/results/baseline.json \  --output   tests/k6/results/comparison_report.md```**Exit codes:**- `0` ÔÇö Sin regresiones, todos los thresholds cumplen- `1` ÔÇö Regresi├│n o fallo de threshold detectado- `2` ÔÇö Error de argumentos o archivos no encontrados## Artefactos Generados| Artefacto | Retenci├│n | Descripci├│n ||-----------|-----------|-------------|| `k6-performance-results-{run_id}` | 90 d├¡as | Resultados de cada ejecuci├│n || `k6-performance-baseline` | 365 d├¡as | Baseline hist├│rico de referencia |## Integraci├│n con Pipeline PrincipalPara usar este workflow como gate en `pipeline-integration.yml`:```yamlperformance-check:  name: "Performance Gate"  uses: ./.github/workflows/performance-smoke.yml  with:    base_url: http://microservice-a:8080    environment: qa```## Seguridad- Actions pinados a commit SHA completo con etiqueta de versi├│n- Permisos m├¡nimos: `contents: read`, `actions: read`- Sin credenciales hardcodeadas- Concurrencia controlada (sin ejecuciones paralelas por entorno)